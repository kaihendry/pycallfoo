[project]
name = "callfoo"
version = "0.1.0"
description = "Depends on Pyunderstand"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pyunderstand>=0.1.0",
]

[tool.uv.sources]
pyunderstand = { git = "https://github.com/kaihendry/pyunderstand.git" }
